interface HeroSectionProps {
  onExploreMenu?: () => void;
}

export default function HeroSection({ onExploreMenu }: HeroSectionProps) {
  return (
    <section className="relative w-full">
      {/* Hero Section - Content removed, now handled by SixtyMinSection below */}
    </section>
  );
}